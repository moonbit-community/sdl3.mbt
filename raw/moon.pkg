import {
  "moonbitlang/core/encoding/utf8" @encoding/utf8,
}

supported_targets = "native"

options(
  "is-main": false,
  link: { "native": { "flags": [ "-lSDL3", "-lSDL3_image" ] } },
  "native-stub": [ "wrap.c" ],
)
